import java.sql.*;

public class Main {
    public static void main(String[] args) throws SQLException {
        /*For implementation testing
        All methods behavior available in Utils Class
        All methods were tested with SQL DataBase
        utils.insert(args);
        utils.delete(args);
        utils.update(args);
        utils.getById(args);
        utils.getAll();
        utils.addNewOrder(args);
        utils.viewAllOrders(args);
        utils.updateOrder(args);
        utils.addComments(args);
        */
        Utils utils = new Utils();
    }
}
